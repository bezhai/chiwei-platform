declare namespace NodeJS {
  interface ProcessEnv {
    REDIS_PASSWORD: string;
    APP_ID: string;
    APP_SECRET: string;
    MONGO_INITDB_ROOT_USERNAME: string;
    MONGO_INITDB_ROOT_PASSWORD: string;
    SELF_CHAT_ID: string;
    HTTP_SECRET: string;
    PROXY_HTTP_SECRET: string;
    REDIS_HOST: string;
    REDIS_PORT: string;
    BANGUMI_ACCESS_TOKEN: string;
    MONGO_HOST: string;
    MONGO_PORT: string;
    MONGO_CONNECT_TIMEOUT_MS: string;
    DOWNLOAD_CRON: string;
    DOWNLOAD_AFTER_ILLUST_INFO_DELAY_MS: string;
    DOWNLOAD_BEFORE_PAGE_DOWNLOAD_DELAY_MS: string;
    DOWNLOAD_AFTER_TASK_DELAY_MS: string;
    DOWNLOAD_AFTER_AUTHOR_DELAY_MS: string;
    DOWNLOAD_LIMITER_COOLDOWN_MS: string;
    RUN_CONNECTIVITY_CHECK: string;
    DISABLE_SCHEDULES: string;
    DISABLE_CONSUMER: string;
    END_POINT: string;
    OSS_ACCESS_KEY_ID: string;
    OSS_ACCESS_KEY_SECRET: string;
    OSS_BUCKET: string;
    MINIO_ENDPOINT: string;
    MINIO_PORT: string;
    MINIO_USE_SSL: string;
    MINIO_ACCESS_KEY: string;
    MINIO_SECRET_KEY: string;
    MINIO_BUCKET: string;
    MINIO_SYNC_ENABLED: string;
    PIXIV_IMAGE_MIRROR_MONGO_ENABLED: string;
    PIXIV_IMAGE_MIRROR_MONGO_HOST: string;
    PIXIV_IMAGE_MIRROR_MONGO_PORT: string;
    PIXIV_IMAGE_MIRROR_MONGO_DATABASE: string;
    PIXIV_IMAGE_MIRROR_MONGO_USERNAME: string;
    PIXIV_IMAGE_MIRROR_MONGO_PASSWORD: string;
    PIXIV_IMAGE_MIRROR_MONGO_AUTH_SOURCE: string;
    PIXIV_IMAGE_MIRROR_MONGO_CONNECT_TIMEOUT_MS: string;
    POST_DOWNLOAD_RECONCILE_ENABLED: string;
    POST_DOWNLOAD_RECONCILE_BATCH_SIZE: string;
    POST_DOWNLOAD_RECONCILE_RETRY_BATCH_SIZE: string;
    POST_DOWNLOAD_RECONCILE_IDLE_DELAY_MS: string;
    POST_DOWNLOAD_RECONCILE_RETRY_DELAY_MS: string;
    POST_DOWNLOAD_RECONCILE_LEASE_MS: string;
    POST_DOWNLOAD_RECONCILE_EPOCH_DELAY_MS: string;
    TAGGER_RESULT_MONGO_ENABLED: string;
    TAGGER_RESULT_MONGO_HOST: string;
    TAGGER_RESULT_MONGO_PORT: string;
    TAGGER_RESULT_MONGO_DATABASE: string;
    TAGGER_RESULT_MONGO_USERNAME: string;
    TAGGER_RESULT_MONGO_PASSWORD: string;
    TAGGER_RESULT_MONGO_AUTH_SOURCE: string;
    TAGGER_RESULT_MONGO_CONNECT_TIMEOUT_MS: string;
    TAGGER_TRIGGER_ENABLED: string;
    TAGGER_ENTRY_URL: string;
    TAGGER_API_TOKEN: string;
    TAGGER_CALLBACK_URL: string;
    TAGGER_CALLBACK_SERVER_ENABLED: string;
    TAGGER_CALLBACK_PORT: string;
    TAGGER_CALLBACK_AUTH_TOKEN: string;
    TAGGER_SUBMIT_BATCH_SIZE: string;
    TAGGER_SUBMIT_TIMEOUT_MS: string;
    TAGGER_SUBMIT_RETRIES: string;
    TAGGER_TRIGGER_WORKER_IDLE_DELAY_MS: string;
    TAGGER_TRIGGER_RETRY_DELAY_MS: string;
    TAGGER_TRIGGER_PROCESSING_TIMEOUT_MS: string;
    TAGGER_TRIGGER_MAX_ATTEMPTS: string;
    TAGGER_SUBMITTED_RECONCILE_AFTER_MS: string;
    TAGGER_SUBMITTED_RECONCILE_LEASE_MS: string;
    TAGGER_SUBMITTED_RECONCILE_RETRY_DELAY_MS: string;
    TAGGER_PROJECTION_ENABLED: string;
    TAGGER_PROJECTION_BACKFILL_ENABLED: string;
    TAGGER_PROJECTION_BATCH_SIZE: string;
    TAGGER_PROJECTION_WORKER_IDLE_DELAY_MS: string;
    TAGGER_PROJECTION_RETRY_DELAY_MS: string;
    TAGGER_PROJECTION_PROCESSING_TIMEOUT_MS: string;
  }
}

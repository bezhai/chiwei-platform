// Re-export state machine from shared
export { StateMachine } from '@inner/shared';
export type {
    StateTransition,
    StateHandler,
    StateMachineContext,
    StateMachineOptions,
} from '@inner/shared';

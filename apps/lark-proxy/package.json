{
  "name": "lark-proxy",
  "version": "1.0.0",
  "description": "Lark event proxy for lane-based routing",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "start": "bun dist/index.js",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@inner/shared": "workspace:*",
    "hono": "^4.7.0",
    "@larksuiteoapi/node-sdk": "^1.23.0",
    "pg": "^8.11.5",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/pg": "^8.11.0",
    "@types/bun": "latest"
  }
}

# R18 Pre-Processing Node Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 pre graph 中新增 R18 内容检测节点，对绫奈（未成年 persona）拦截，对赤尾和千凪仅记录放行。

**Architecture:** 新增 `check_r18_content` 节点并行加入现有 pre graph。节点本身根据 `persona_id` 决定拦截或放行，无需改动 aggregate 逻辑。`persona_id` 从 `stream_chat` 透传到 `run_pre`。

**Tech Stack:** LangGraph node, Langfuse prompt (chat type), Pydantic structured output, pytest

---

### Task 1: 扩展 PreState 和 BlockReason

**Files:**
- Modify: `apps/agent-service/app/agents/graphs/pre/state.py:10-46`

- [ ] **Step 1: Write the failing test**

```python
# tests/unit/test_pre_state.py — 追加到文件末尾

class TestR18BlockReason:
    """R18_CONTENT 枚举值存在"""

    def test_r18_content_enum_exists(self):
        assert BlockReason.R18_CONTENT == "r18_content"

    def test_pre_state_has_persona_id(self):
        """PreState 包含 persona_id 字段"""
        state: PreState = {
            "message_content": "test",
            "persona_id": "akao",
            "safety_results": [],
            "is_blocked": False,
            "block_reason": None,
        }
        assert state["persona_id"] == "akao"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd apps/agent-service && uv run pytest tests/unit/test_pre_state.py::TestR18BlockReason -v`
Expected: FAIL — `BlockReason` has no `R18_CONTENT`, `PreState` has no `persona_id`

- [ ] **Step 3: Implement — add R18_CONTENT and persona_id**

`apps/agent-service/app/agents/graphs/pre/state.py` — 两处改动：

```python
class BlockReason(str, Enum):
    """拦截原因"""

    BANNED_WORD = "banned_word"
    PROMPT_INJECTION = "prompt_injection"
    SENSITIVE_POLITICS = "sensitive_politics"
    R18_CONTENT = "r18_content"  # 新增
```

```python
class PreState(TypedDict):
    """Pre Graph 状态"""

    # 输入
    message_content: str
    persona_id: str  # 新增：当前处理的 persona

    # 安全检测结果（并行合并）
    safety_results: Annotated[list[SafetyResult], merge_safety_results]

    # 最终输出
    is_blocked: bool
    block_reason: BlockReason | None
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd apps/agent-service && uv run pytest tests/unit/test_pre_state.py -v`
Expected: ALL PASS

- [ ] **Step 5: Commit**

```bash
git add apps/agent-service/app/agents/graphs/pre/state.py apps/agent-service/tests/unit/test_pre_state.py
git commit -m "feat(pre): add R18_CONTENT block reason and persona_id to PreState"
```

---

### Task 2: 创建 R18 检测节点

**Files:**
- Create: `apps/agent-service/app/agents/graphs/pre/nodes/r18_safety.py`
- Modify: `apps/agent-service/app/agents/graphs/pre/nodes/__init__.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/unit/test_r18_safety.py
"""test_r18_safety.py — R18 检测节点单元测试"""

from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from app.agents.graphs.pre.state import BlockReason, SafetyResult

pytestmark = pytest.mark.unit


class TestCheckR18Content:
    """R18 内容检测节点"""

    async def test_r18_detected_minor_persona_blocked(self):
        """R18 内容 + 未成年 persona → 拦截"""
        from app.agents.graphs.pre.nodes.r18_safety import (
            R18CheckResult,
            check_r18_content,
        )

        mock_model = MagicMock()
        mock_structured = MagicMock()
        mock_structured.ainvoke = AsyncMock(
            return_value=R18CheckResult(is_r18=True, confidence=0.9)
        )
        mock_model.with_structured_output.return_value = mock_structured

        state = {
            "message_content": "一些 R18 内容",
            "persona_id": "ayana",
            "safety_results": [],
        }

        with (
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=mock_model,
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
        ):
            result = await check_r18_content(state, config={"callbacks": []})

        safety = result["safety_results"][0]
        assert safety.blocked is True
        assert safety.reason == BlockReason.R18_CONTENT

    async def test_r18_detected_adult_persona_logged_not_blocked(self):
        """R18 内容 + 成年 persona → 记录但放行"""
        from app.agents.graphs.pre.nodes.r18_safety import (
            R18CheckResult,
            check_r18_content,
        )

        mock_model = MagicMock()
        mock_structured = MagicMock()
        mock_structured.ainvoke = AsyncMock(
            return_value=R18CheckResult(is_r18=True, confidence=0.9)
        )
        mock_model.with_structured_output.return_value = mock_structured

        state = {
            "message_content": "一些 R18 内容",
            "persona_id": "akao",
            "safety_results": [],
        }

        with (
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=mock_model,
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
        ):
            result = await check_r18_content(state, config={"callbacks": []})

        safety = result["safety_results"][0]
        assert safety.blocked is False
        assert "r18_logged" in (safety.detail or "")

    async def test_no_r18_content_passes(self):
        """非 R18 内容 → 放行"""
        from app.agents.graphs.pre.nodes.r18_safety import (
            R18CheckResult,
            check_r18_content,
        )

        mock_model = MagicMock()
        mock_structured = MagicMock()
        mock_structured.ainvoke = AsyncMock(
            return_value=R18CheckResult(is_r18=False, confidence=0.1)
        )
        mock_model.with_structured_output.return_value = mock_structured

        state = {
            "message_content": "正常聊天内容",
            "persona_id": "ayana",
            "safety_results": [],
        }

        with (
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=mock_model,
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
        ):
            result = await check_r18_content(state, config={"callbacks": []})

        safety = result["safety_results"][0]
        assert safety.blocked is False

    async def test_llm_failure_passes(self):
        """LLM 异常 → fail-open 放行"""
        from app.agents.graphs.pre.nodes.r18_safety import check_r18_content

        state = {
            "message_content": "任何内容",
            "persona_id": "ayana",
            "safety_results": [],
        }

        with (
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                side_effect=RuntimeError("LLM down"),
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
        ):
            result = await check_r18_content(state, config={"callbacks": []})

        safety = result["safety_results"][0]
        assert safety.blocked is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd apps/agent-service && uv run pytest tests/unit/test_r18_safety.py -v`
Expected: FAIL — module `r18_safety` does not exist

- [ ] **Step 3: Implement the R18 detection node**

Create `apps/agent-service/app/agents/graphs/pre/nodes/r18_safety.py`:

```python
"""R18 内容检测节点

对未成年 persona（绫奈）拦截 R18 内容，对成年 persona 仅记录放行。
"""

import logging

from pydantic import BaseModel, Field

from app.agents.graphs.pre.state import BlockReason, PreState, SafetyResult
from app.agents.infra.langfuse_client import get_prompt
from app.agents.infra.model_builder import ModelBuilder

logger = logging.getLogger(__name__)

# 需要拦截 R18 内容的 persona（未成年）
R18_BLOCKED_PERSONAS = frozenset({"ayana"})


class R18CheckResult(BaseModel):
    """R18 内容检测结果"""

    is_r18: bool = Field(description="是否包含色情、性暗示或其他成人内容")
    confidence: float = Field(description="置信度 0-1", ge=0, le=1)


async def check_r18_content(state: PreState, config) -> dict:
    """R18 内容检测节点

    - 未成年 persona + R18 → 拦截
    - 成年 persona + R18 → 记录，放行
    - 非 R18 → 放行
    """
    message = state["message_content"]
    persona_id = state["persona_id"]

    try:
        langfuse_prompt = get_prompt("guard_r18_content")
        messages = langfuse_prompt.compile(message=message)

        model = await ModelBuilder.build_chat_model(
            "guard-model", reasoning_effort="low"
        )
        structured_model = model.with_structured_output(R18CheckResult)

        result: R18CheckResult = await structured_model.ainvoke(
            messages, config=config
        )

        if result.is_r18 and result.confidence >= 0.75:
            if persona_id in R18_BLOCKED_PERSONAS:
                logger.warning(
                    f"R18 内容拦截: persona={persona_id}, confidence={result.confidence}"
                )
                return {
                    "safety_results": [
                        SafetyResult(
                            blocked=True,
                            reason=BlockReason.R18_CONTENT,
                            detail=f"confidence={result.confidence}",
                        )
                    ]
                }
            else:
                logger.info(
                    f"R18 内容记录(放行): persona={persona_id}, confidence={result.confidence}"
                )
                return {
                    "safety_results": [
                        SafetyResult(
                            blocked=False,
                            detail=f"r18_logged:persona={persona_id},confidence={result.confidence}",
                        )
                    ]
                }

        return {"safety_results": [SafetyResult(blocked=False)]}

    except Exception as e:
        logger.error(f"R18 内容检测失败: {e}")
        return {"safety_results": [SafetyResult(blocked=False)]}
```

Update `apps/agent-service/app/agents/graphs/pre/nodes/__init__.py`:

```python
"""Pre Graph 节点"""

from app.agents.graphs.pre.nodes.r18_safety import check_r18_content
from app.agents.graphs.pre.nodes.safety import (
    aggregate_results,
    check_banned_word_node,
    check_prompt_injection,
    check_sensitive_politics,
)

__all__ = [
    "check_banned_word_node",
    "check_prompt_injection",
    "check_sensitive_politics",
    "check_r18_content",
    "aggregate_results",
]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd apps/agent-service && uv run pytest tests/unit/test_r18_safety.py -v`
Expected: ALL PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add apps/agent-service/app/agents/graphs/pre/nodes/r18_safety.py apps/agent-service/app/agents/graphs/pre/nodes/__init__.py apps/agent-service/tests/unit/test_r18_safety.py
git commit -m "feat(pre): add R18 content detection node with persona-aware blocking"
```

---

### Task 3: 创建 Langfuse guard_r18_content Prompt

**Files:** Langfuse API（无本地文件改动）

- [ ] **Step 1: 创建 prompt**

```bash
python3 ~/.claude/skills/langfuse/scripts/langfuse_api.py create-prompt '{
  "name": "guard_r18_content",
  "type": "chat",
  "prompt": [
    {
      "role": "system",
      "content": "你是一个内容安全检测助手。判断用户消息是否包含以下任何一类成人内容：\n\n1. **色情内容**：性行为描写、性暗示、色情对话\n2. **露骨身体描写**：以性意味描述身体部位\n3. **性骚扰**：带有性意味的骚扰、调戏\n\n## 不算 R18 的情况\n- 正常的恋爱话题（表白、约会、喜欢某人）\n- 生理健康知识讨论\n- 文学作品中的非露骨情感描写\n- 开玩笑但不含实质色情内容\n\n请基于消息的实际内容判断，不要过度解读。"
    },
    {
      "role": "user",
      "content": "请分析以下用户消息：\n\n{{message}}"
    }
  ],
  "labels": ["production", "latest"]
}'
```

- [ ] **Step 2: 验证 prompt 已创建**

```bash
python3 ~/.claude/skills/langfuse/scripts/langfuse_api.py get-prompt '{"name":"guard_r18_content","label":"production"}'
```

Expected: 返回刚创建的 prompt 内容

- [ ] **Step 3: Commit（无文件改动，仅记录）**

无需 git commit，Langfuse prompt 是运行时配置。

---

### Task 4: 将 R18 节点接入 Pre Graph

**Files:**
- Modify: `apps/agent-service/app/agents/graphs/pre/graph.py:1-111`

- [ ] **Step 1: Modify graph to include R18 node**

`apps/agent-service/app/agents/graphs/pre/graph.py` — 三处改动：

1. import 新增 `check_r18_content`:

```python
from app.agents.graphs.pre.nodes import (
    aggregate_results,
    check_banned_word_node,
    check_prompt_injection,
    check_r18_content,
    check_sensitive_politics,
)
```

2. `_create_pre_graph` 中新增节点和边：

```python
    # 安全检测节点（并行）
    builder.add_node("check_banned_word", check_banned_word_node)
    builder.add_node("check_prompt_injection", check_prompt_injection)
    builder.add_node("check_sensitive_politics", check_sensitive_politics)
    builder.add_node("check_r18_content", check_r18_content)  # 新增

    # 聚合节点
    builder.add_node("aggregate", aggregate_results)

    # 从 START 并行分发
    builder.add_edge(START, "check_banned_word")
    builder.add_edge(START, "check_prompt_injection")
    builder.add_edge(START, "check_sensitive_politics")
    builder.add_edge(START, "check_r18_content")  # 新增

    # 汇聚到 aggregate
    builder.add_edge("check_banned_word", "aggregate")
    builder.add_edge("check_prompt_injection", "aggregate")
    builder.add_edge("check_sensitive_politics", "aggregate")
    builder.add_edge("check_r18_content", "aggregate")  # 新增
```

3. `run_pre` 签名加 `persona_id` 参数：

```python
async def run_pre(message_content: str, persona_id: str = "") -> PreState:
    """运行 Pre 处理图

    Args:
        message_content: 待处理的消息内容
        persona_id: 当前处理的 persona（用于 R18 persona-aware 判断）

    Returns:
        PreState: 包含安全检测结果
    """
    graph = get_pre_graph()

    config = {
        "callbacks": [CallbackHandler()],
        "run_name": "pre",
    }

    initial_state: PreState = {
        "message_content": message_content,
        "persona_id": persona_id,
        "safety_results": [],
        "is_blocked": False,
        "block_reason": None,
    }

    result = await graph.ainvoke(initial_state, config=config)
    return result
```

4. 更新 docstring 中的图结构注释（加入 r18_content 节点）。

- [ ] **Step 2: Run existing tests to verify no regression**

Run: `cd apps/agent-service && uv run pytest tests/unit/test_pre_state.py tests/unit/test_r18_safety.py -v`
Expected: ALL PASS

- [ ] **Step 3: Commit**

```bash
git add apps/agent-service/app/agents/graphs/pre/graph.py
git commit -m "feat(pre): wire R18 check node into pre graph parallel fan-out"
```

---

### Task 5: 从 stream_chat 透传 persona_id 到 run_pre

**Files:**
- Modify: `apps/agent-service/app/agents/domains/main/agent.py:87`

- [ ] **Step 1: Update the run_pre call site**

In `apps/agent-service/app/agents/domains/main/agent.py`, line 87, change:

```python
# Before:
pre_task = asyncio.create_task(run_pre(parsed.render()))

# After:
pre_task = asyncio.create_task(run_pre(parsed.render(), persona_id=effective_persona))
```

`effective_persona` is already defined on line 83:
```python
effective_persona = persona_id or header_vars["app_name"].get() or ""
```

- [ ] **Step 2: Verify existing tests still pass**

Run: `cd apps/agent-service && uv run pytest tests/ -v --timeout=30 -x`
Expected: ALL PASS

- [ ] **Step 3: Commit**

```bash
git add apps/agent-service/app/agents/domains/main/agent.py
git commit -m "feat(pre): pass persona_id to run_pre for R18 persona-aware filtering"
```

---

### Task 6: 集成测试 — R18 节点在 Pre Graph 中的行为

**Files:**
- Modify: `apps/agent-service/tests/integration/test_pre_graph.py`

- [ ] **Step 1: Add R18 integration tests**

追加到 `tests/integration/test_pre_graph.py` 末尾：

```python
class TestPreGraphR18Content:
    """R18 内容检测集成测试"""

    async def test_r18_content_blocks_minor_persona(self):
        """R18 内容 + 未成年 persona → 拦截"""
        from app.agents.graphs.pre.nodes.r18_safety import R18CheckResult
        from app.agents.graphs.pre.nodes.safety import (
            PoliticsCheckResult,
            PromptInjectionResult,
        )

        result_map = {
            PromptInjectionResult: PromptInjectionResult(
                is_injection=False, confidence=0.1
            ),
            PoliticsCheckResult: PoliticsCheckResult(
                is_sensitive=False, confidence=0.1
            ),
            R18CheckResult: R18CheckResult(is_r18=True, confidence=0.9),
        }

        # complexity node 可能存在也可能不存在，用 try 处理
        try:
            from app.agents.graphs.pre.nodes.complexity import (
                ComplexityClassification,
            )
            result_map[ComplexityClassification] = ComplexityClassification(
                complexity="simple", confidence=0.9
            )
        except ImportError:
            pass

        smart_model = _make_smart_model(result_map)

        with (
            patch(
                "app.agents.graphs.pre.nodes.safety.check_banned_word",
                new_callable=AsyncMock,
                return_value=None,
            ),
            patch(
                "app.agents.graphs.pre.nodes.safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=smart_model,
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=smart_model,
            ),
            patch(
                "app.agents.graphs.pre.nodes.safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
            patch(
                "app.agents.graphs.pre.graph.CallbackHandler",
                return_value=MagicMock(),
            ),
        ):
            # complexity node patch (if exists)
            with patch(
                "app.agents.graphs.pre.nodes.complexity.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=smart_model,
            ), patch(
                "app.agents.graphs.pre.nodes.complexity.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ):
                from app.agents.graphs.pre.graph import run_pre

                result = await run_pre("R18 内容", persona_id="ayana")

        assert result["is_blocked"] is True
        assert result["block_reason"] == BlockReason.R18_CONTENT

    async def test_r18_content_passes_adult_persona(self):
        """R18 内容 + 成年 persona → 放行"""
        from app.agents.graphs.pre.nodes.r18_safety import R18CheckResult
        from app.agents.graphs.pre.nodes.safety import (
            PoliticsCheckResult,
            PromptInjectionResult,
        )

        result_map = {
            PromptInjectionResult: PromptInjectionResult(
                is_injection=False, confidence=0.1
            ),
            PoliticsCheckResult: PoliticsCheckResult(
                is_sensitive=False, confidence=0.1
            ),
            R18CheckResult: R18CheckResult(is_r18=True, confidence=0.9),
        }

        try:
            from app.agents.graphs.pre.nodes.complexity import (
                ComplexityClassification,
            )
            result_map[ComplexityClassification] = ComplexityClassification(
                complexity="simple", confidence=0.9
            )
        except ImportError:
            pass

        smart_model = _make_smart_model(result_map)

        with (
            patch(
                "app.agents.graphs.pre.nodes.safety.check_banned_word",
                new_callable=AsyncMock,
                return_value=None,
            ),
            patch(
                "app.agents.graphs.pre.nodes.safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=smart_model,
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=smart_model,
            ),
            patch(
                "app.agents.graphs.pre.nodes.safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
            patch(
                "app.agents.graphs.pre.nodes.r18_safety.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ),
            patch(
                "app.agents.graphs.pre.graph.CallbackHandler",
                return_value=MagicMock(),
            ),
        ):
            with patch(
                "app.agents.graphs.pre.nodes.complexity.ModelBuilder.build_chat_model",
                new_callable=AsyncMock,
                return_value=smart_model,
            ), patch(
                "app.agents.graphs.pre.nodes.complexity.get_prompt",
                return_value=MagicMock(
                    compile=MagicMock(return_value="mocked_messages")
                ),
            ):
                from app.agents.graphs.pre.graph import run_pre

                result = await run_pre("R18 内容", persona_id="akao")

        assert result["is_blocked"] is False
```

- [ ] **Step 2: Run all pre-related tests**

Run: `cd apps/agent-service && uv run pytest tests/unit/test_pre_state.py tests/unit/test_r18_safety.py tests/integration/test_pre_graph.py -v`
Expected: ALL PASS

- [ ] **Step 3: Commit**

```bash
git add apps/agent-service/tests/integration/test_pre_graph.py
git commit -m "test(pre): add R18 content detection integration tests"
```
